You are reviewing /home/admin/bitchtea/MIGRATION_PLAN.md (v3). This is the
third revision. v2 received "accept-with-revisions" — v3 incorporates all
seven of those revisions. Your job is to confirm the plan is now ready to
implement, or flag remaining blockers.

The plan migrates bitchtea (a Charm-stack TUI agent) from a hand-rolled
internal/llm/ (deleted) to charm.land/fantasy v0.17.1 + charm.land/catwalk
v0.35.1, mirroring the architecture of charm.land/crush. fantasy owns the
agent loop; bitchtea provides callbacks and AgentTool wrappers around its
existing internal/tools/Registry.Execute.

Verify ALL of these against fantasy v0.17.1 source at
/home/admin/go/pkg/mod/charm.land/fantasy@v0.17.1/:

1. **API names + signatures.** fantasy.NewAgent, AgentStreamCall,
   MessageRoleSystem/User/Assistant/Tool, TextPart, ToolCallPart,
   ToolResultPart, ToolResultOutputContent(Text), AgentTool interface,
   ToolInfo, ToolCallContent, ToolResultContent, ProviderError. Callback
   names: OnTextDelta, OnReasoningDelta, OnToolCall, OnToolResult,
   OnStreamFinish, OnError. Stop conditions: WithStopConditions +
   StepCountIs(n) (NOT WithMaxSteps).

2. **Tool wrapper behavior.** Plan says Run returns
   fantasy.NewTextErrorResponse(...) for normal tool failures because
   returning a Go error aborts the whole stream. Confirm this against
   fantasy/agent.go and tool.go.

3. **Transcript construction.** Plan walks result.Steps[].Messages AFTER
   Stream returns to rebuild bitchtea's []llm.Message — NOT mutating
   a.messages from inside Run (race with fantasy's tool dispatcher
   goroutine). Confirm this matches what fantasy actually exposes
   post-Stream.

4. **Provider routing (D4).** Plan uses net/url parse + comparison against
   openai.DefaultURL / openrouter.DefaultURL / vercel.DefaultURL /
   anthropic.DefaultURL constants. Confirm these constants exist and that
   the routing covers all built-in profiles in internal/config/config.go
   (ollama, openrouter, aihubmix, avian, copilot, cortecs, huggingface,
   ionet, nebius, synthetic, venice, vercel, xai, zai-openai, zai-anthropic).

5. **Anthropic /v1 prefix.** fantasy's anthropic.DefaultURL is
   "https://api.anthropic.com" (no /v1). The zai-anthropic profile baseURL
   is "https://api.z.ai/api/anthropic". Confirm plan handles this without
   double-prefixing.

6. **Cache invalidation (D5).** Plan adds a mutex to Client and invalidates
   cached provider+model on every Set*. SetDebugHook only nils debugHook.
   Confirm this is correct vs over-invalidation, and that nil->nil
   SetDebugHook calls are no-ops.

7. **Event package split.** internal/agent/event subpackage holds Event +
   State to break the llm->agent cycle. internal/agent keeps type aliases
   (type Event = event.Event, type State = event.State) for gradual
   migration. Confirm no caller breaks.

8. **Test plan (step 9).** Plan explicitly recovers deleted internal/llm
   tests from git history (HEAD~ before deletion). Confirm the test
   recovery list is complete and that mandatory tests (cache invalidation,
   provider routing, transcript shape) are included.

9. **Anything dangerous.** New unflagged risks. Sequence ordering. Test
   coverage gaps. Out-of-scope items that should actually be in scope.

Verdict: accept / accept-with-revisions / reject. If anything but accept,
list specific changes. Be terse and concrete. Quote line references when
applicable.
