# Decisions

- Use `brain/` as the durable working memory for this repo.
- Treat conversation notes as tentative by default.
- Keep one small next action in `NEXT_STEPS.md`.
- Read `brain/` first when resuming work.
- Use Kanban only after the idea is clear and execution should begin.
- Keep `brain/PROJECT_MAP.md` as the canonical high-level map of the codebase.
- Keep conversation notes, current state, and decisions distinct so restarts do not blur them together.
